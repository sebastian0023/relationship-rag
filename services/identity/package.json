{
  "name": "@relationship-rag/identity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "provision": "tsx src/provision.ts"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "3.1017.0",
    "@aws-sdk/client-dynamodb": "3.1017.0",
    "@aws-sdk/lib-dynamodb": "3.1017.0",
    "@relationship-rag/contracts": "0.1.0",
    "@relationship-rag/domain": "0.1.0",
    "@relationship-rag/observability": "0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
