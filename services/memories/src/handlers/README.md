# Memory handlers

Phase 2 adds thin API Gateway handlers here. Handlers validate contracts, derive the authenticated
actor, invoke one application use case, and translate known errors to safe HTTP responses.
